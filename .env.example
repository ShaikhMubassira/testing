# Server Port (Render will provide this automatically; optional for local dev)
PORT=3000

# CORS Origin (allow all for testing; restrict in production)
CORS_ORIGIN=*

# Optional: Set to "production" for deployment logging
NODE_ENV=development

# Request timeout in milliseconds
REQUEST_TIMEOUT_MS=60000
